# Rust
/target
**/*.rs.bk
# Cargo.lock IS committed: molde ships a binary, so we pin the exact
# dependency graph for reproducible builds.

# .NET sidecar (legacy, no longer part of the build)
[Bb]in/
[Oo]bj/
*.user
nupkg/

# Editors
.idea/
.vscode/
*.swp

# Local env / scratch
.env
*.local
scratch/
_scaffold_review/
sync-*.sql

# Internal development artifacts (not part of the public project)
handoffs/
.claude/
